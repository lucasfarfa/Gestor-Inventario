# Gestor-Inventario

App de Gestion de inventario CLI desarrolada con C# para .NET

La idea es que escale a una web api con ASP.NET Core y una interfaz web con Blazor.

Uso de JSONSerializer (primera vez) para "persitencia"
y graba un logfile cada vez que se realiza un movimiento